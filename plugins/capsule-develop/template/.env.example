# 一般情況你不用自己動這個檔——`/new-project` 會用你的 access token 自動產生 .env 並填好值。
# 這份是「手動 fallback / 給工程師」用的範本。
#
# 複製這個檔成 .env，填入你自己的 Supabase 專案憑證。
# .env 已被 gitignore，不會進版控。這兩個值都是「公開等級」的前端金鑰（不是 service key）。
#
# 從哪裡拿：Supabase Dashboard -> 你的專案 -> Project Settings -> API Keys
#   Project URL             -> NUXT_PUBLIC_SUPABASE_URL
#   anon（publishable）key  -> NUXT_PUBLIC_SUPABASE_ANON_KEY

NUXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
NUXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# 這個 App 顯示在畫面上的名字
NUXT_PUBLIC_APP_NAME=CAPSULE MVP

# --- 整合測試用（選配）---
# 若要在本機跑 npm run test:integration，需要 service_role key（繞過 RLS，只放本機 .env，切勿進版控）
# SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
# SUPABASE_SERVICE_KEY=your-service-role-key
